/**
 * Versão exibida no app (sidebar).
 * Incrementada a cada publicação (+1 no PATCH).
 * Quando PATCH chegar a 100: PATCH → 0 e MINOR +1 (ex.: 1.0.99 → 1.1.0).
 */
export const APP_VERSION = '1.0.97';
